export enum CycleStatus {
    IN_PROGRESS = 'in-progress',
    COMPLETED = 'completed',
    CANCELLED = 'cancelled',
    FAILURE = 'failure'
  }
  
  export enum DeviceType {
    WASHER = 'washer',
    DRYER = 'dryer'
  }